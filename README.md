SwiftScript
===========

Run Swift code in JavaScript environment (e.g. web browser or nodeJS). SwiftScript is compiler and interpreter for The Swift Programming Language implemented in JavaScript and for JavaScript.
